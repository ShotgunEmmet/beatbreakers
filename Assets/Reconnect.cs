using UnityEngine;
using System.Collections;

public class Reconnect : MonoBehaviour {

	void OnClick(){
		//attempt to reconnect if success then reload this scene
			//GameGlobals.online = true;
			//Application.LoadLevel(Application.loadedLevel);
	}
}
